import { useLocale } from '@i18n/useLocale'

export const ROUTES = {
	home: '/',
	menu: '/menu',
	addresses: '/addresses',
	events: '/events',
	about: '/about'
}
export type AppRoutes = typeof ROUTES
